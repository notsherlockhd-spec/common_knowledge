---

# 算法思想总纲（Algorithm Knowledge Base）

---

# 一、如何使用这份文档

做题时永远先问两件事：

```
① 数据结构长什么样？（数组 / 树 / 图 / 网格 / 字符串）
② 题目本质在问什么？（搜索 / 连通 / 最短路 / 依赖 / 优化）
```

算法 = **数据结构 × 算法思想**

---

# 二、数据结构总分类

```
数据结构
│
├── 线性结构
│     ├── 数组
│     ├── 链表
│     ├── 栈
│     └── 队列
│
├── 树结构
│     ├── 二叉树
│     ├── 二叉搜索树 BST
│     ├── 堆（优先队列）
│     └── 平衡树（进阶）
│
├── 图结构
│     ├── 普通图
│     ├── 网格图（隐式图）
│     └── DAG 有向无环图
│
├── 集合结构
│     ├── 哈希表
│     └── 并查集
│
├── 字符串结构
│     ├── 字符数组
│     ├── Trie
│     └── KMP（匹配结构）
│
└── 区间结构（高级）
      ├── 前缀和
      ├── 树状数组
      └── 线段树
```

---

# 三、算法思想总分类

```
算法思想
│
├── 搜索问题
│     ├── DFS
│     └── BFS
│
├── 连通性问题
│     └── 并查集
│
├── 路径问题
│     ├── BFS最短路（无权图）
│     ├── Dijkstra（单源最短路）
│     ├── Bellman-Ford（可负权）
│     └── Floyd（多源最短路）
│
├── 依赖关系问题
│     └── 拓扑排序
│
├── 最优子结构问题
│     └── 动态规划 DP
│
├── 局部最优策略
│     └── 贪心
│
├── 区间问题
│     ├── 前缀和
│     ├── 差分
│     └── 滑动窗口
│
└── 数据维护问题
      ├── 堆 / 优先队列
      ├── 单调栈 / 单调队列
      └── 线段树 / 树状数组
```


# 四、数据结构细节

## 1. 线性结构

特点：

```
元素排成一条线
```

---

### 1.1 数组（Array）

最基础的数据结构。

#### 特点

```
连续内存
O(1) 随机访问
```

#### 常见题型

```
双指针
滑动窗口
前缀和
二分查找
```

---

### 1.2 链表（Linked List）

#### 特点

```
不连续内存
通过指针连接
```

#### 常见题型

```
反转链表
合并链表
快慢指针
```

---

### 1.3 栈（Stack）

```
后进先出 LIFO
```

#### 用途

```
括号匹配
表达式计算
单调栈
```

C实现（应试版）

```c
int st[N], top = -1;
st[++top] = x;   // push
top--;           // pop
```

---

### 1.4 队列（Queue）

```
先进先出 FIFO
```

#### 用途

```
BFS
滑动窗口
```

C实现（循环队列）

```c
int q[N], head=0, tail=0;
q[tail++] = x;
x = q[head++];
```

---

## 2. 树结构

关键理解：

```
树 = 无环图
```

---

### 2.1 二叉树（Binary Tree）

每个节点最多两个孩子。

#### 常见操作

```
前序遍历
中序遍历
后序遍历
层序遍历（BFS）
```

遍历模板：

```c
void dfs(TreeNode* root){
    if(!root) return;
    dfs(root->left);
    dfs(root->right);
}
```

---

### 2.2 二叉搜索树（BST）

性质：

```
左 < 根 < 右
```

#### 核心用途

```
有序数据
查找 / 插入 / 删除
```

---

### 2.3 堆（Heap / Priority Queue）

本质：

```
完全二叉树
```

#### 类型

```
最小堆
最大堆
```

#### 用途

```
TopK
Dijkstra
合并K链表
```

数组实现：

```c
int heap[N], size;
```

---

### 2.4 平衡树（了解）

```
AVL / 红黑树
```

刷题很少手写，了解即可。

---

## 3. 图结构

最重要的数据结构之一。

---

### 3.1 图的表示方式

#### 邻接矩阵（稠密图）

```c
int g[N][N];
```

#### 邻接表（稀疏图）⭐

```c
vector<int> adj[N];
```

---

### 3.2 图的类型

```
无向图
有向图
带权图
DAG
```

---

### 3.3 特殊图：网格图

二维数组本质是图。

```
上 下 左 右 = 边
格子 = 节点
```

经典题：

```
岛屿数量
最短路径
迷宫
```

---

## 4. 集合结构

---

### 4.1 哈希表（HashMap）

作用：

```
快速查找
去重
统计频率
```

复杂度：

```
平均 O(1)
```

---

### 4.2 并查集（Union-Find）

用途：

```
集合合并
判断连通
```

既属于集合，也属于图。

---

## 5. 字符串结构

本质：

```
字符数组
```

---

### 5.1 Trie（前缀树）

用于：

```
前缀匹配
字典查找
```

---

### 5.2 KMP（模式匹配）

解决：

```
字符串查找
```

---

## 6. 区间结构（高级数据结构）

---

### 6.1 前缀和

```
快速求区间和
```

```c
sum[i] = sum[i-1] + a[i];
```

---

### 6.2 树状数组（Fenwick Tree）

支持：

```
单点修改
区间查询
```

复杂度：

```
O(log n)
```

---

### 6.3 线段树

支持：

```
区间修改
区间查询
```

复杂度：

```
O(log n)
```

---



# 五、算法思想细节

## 1. 搜索类算法（Search）

搜索 = **遍历状态空间**

---

### 1.1 DFS（深度优先搜索）

#### 适用场景

```
枚举所有可能
连通块
回溯
树遍历
```

#### 核心思想

一路走到黑 → 回溯

#### 模板（图遍历）

```c
void dfs(int u) {
    visited[u] = 1;
    for (each v in adj[u]) {
        if (!visited[v]) {
            dfs(v);
        }
    }
}
```

---

### 1.2 回溯（DFS 的子集）

用于 **组合 / 排列 / 子集**

#### 模板

```c
void dfs() {
    if (达到终点) {
        保存答案;
        return;
    }

    for (所有选择) {
        做选择;
        dfs();
        撤销选择;
    }
}
```

---

### 1.3 BFS（广度优先搜索）

#### 适用场景

```
最少步数
最少操作
无权最短路
```

#### 模板

```c
queue q;
q.push(start);
dist[start] = 0;

while (!q.empty()) {
    u = q.front(); q.pop();

    for (v : adj[u]) {
        if (!visited[v]) {
            visited[v] = 1;
            dist[v] = dist[u] + 1;
            q.push(v);
        }
    }
}
```

---

### 1.4 多源 BFS

多个起点同时扩散。

```c
for (所有起点) q.push(起点);
```

---

## 2. 连通性算法（Union-Find）

解决问题本质：

```
是否在同一集合？
是否连通？
```

---

### 并查集模板

```c
int parent[N];

int find(int x) {
    if (parent[x] != x)
        parent[x] = find(parent[x]);
    return parent[x];
}

void unionSet(int a, int b) {
    parent[find(a)] = find(b);
}
```

---

#### 典型用途

```
连通块数量
检测环
最小生成树
```

---

## 3. 路径算法（Shortest Path）

---

### 3.1 BFS 最短路（无权图）

条件：

```
所有边权 = 1
```

使用：BFS

---

### 3.2 Dijkstra（单源最短路）

条件：

```
边权 ≥ 0
```

#### 模板（优先队列）

```c
priority_queue pq;
dist[start] = 0;
pq.push({0, start});

while (!pq.empty()) {
    (d,u) = pq.pop();

    if (d > dist[u]) continue;

    for (v,w in adj[u]) {
        if (dist[v] > dist[u] + w) {
            dist[v] = dist[u] + w;
            pq.push({dist[v], v});
        }
    }
}
```

复杂度：

```
O(E log V)
```

---

### 3.3 Bellman-Ford

处理 **负权边**

```
重复松弛 N-1 次
```

---

### 3.4 Floyd（全源最短路）

```
求任意两点最短路
```

#### 模板

```c
for k
  for i
    for j
      dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j]);
```

复杂度：

```
O(n³)
```

---

## 4. 拓扑排序（DAG）

解决：

```
依赖关系
课程安排
任务顺序
```

---

### BFS拓扑排序模板

```c
queue q;
for (所有入度为0) 入队

while (!q.empty()) {
    u = q.front(); q.pop();
    for (v : adj[u]) {
        if (--indegree[v] == 0)
            q.push(v);
    }
}
```

---

## 5. 动态规划（DP）

核心：

```
大问题 = 小问题
```

三步：

```
1. 定义状态
2. 写转移方程
3. 初始化
```

经典类型：

| 类型     | 例子      |
| ------ | ------- |
| 线性DP   | 爬楼梯     |
| 背包DP   | 0/1背包   |
| 区间DP   | 括号匹配    |
| 状态压缩DP | bitmask |

---

## 6. 贪心算法

核心：

```
每一步都选当前最优
```

识别关键词：

```
最少数量
最多数量
最小代价
区间问题
```

经典问题：

```
区间调度
最小生成树
霍夫曼编码
```

---

## 7. 区间技巧

---

### 前缀和

```
快速求区间和
```

```c
sum[i] = sum[i-1] + a[i];
区间和 = sum[r] - sum[l-1];
```

---

### 差分

```
快速区间修改
```

```c
diff[l] += x;
diff[r+1] -= x;
```

---

### 滑动窗口

```
连续子数组问题
```

---

## 8. 数据维护结构

---

### 堆（优先队列）

用途：

```
Top K
最小值/最大值
Dijkstra
```

---

### 单调栈

用途：

```
下一个更大元素
柱状图面积
```

---

### 单调队列

用途：

```
滑动窗口最大值
```

---

### 线段树 / 树状数组

用途：

```
动态区间查询
动态区间更新
```

---

# 六、最终总结（最重要）

刷题时先判断：

```
是否在搜索？ → DFS/BFS
是否在判断连通？ → 并查集
是否在求最短路？ → BFS/Dijkstra/Floyd
是否在处理依赖？ → 拓扑排序
是否在做优化？ → DP
是否在做区间？ → 前缀和/滑窗
```

---

**算法导航地图**。

